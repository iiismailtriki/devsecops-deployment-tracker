pipeline {
    agent any

    stages {
        stage('Checkout Test') {
            steps {
                echo 'Jenkinsfile loaded from Git successfully!'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
